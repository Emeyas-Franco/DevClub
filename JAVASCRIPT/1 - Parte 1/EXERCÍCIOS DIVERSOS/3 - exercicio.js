// 3 -> Crie uma variável const chamada itemPrice e atribua a ela um número decimal (por exemplo, 29.99). Crie outra variável const chamada quantity e atribua a ela um número inteiro (por exemplo, 3). Calcule o totalCost multiplicando itemPrice por quantity e imprima totalCost no console.

const itemPrice = 36.59
const quantity = 7
const totalCost = itemPrice * quantity

console.log(totalCost)