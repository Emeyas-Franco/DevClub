// 12 -> Crie um array const chamado colors contendo pelo menos três nomes de cores diferentes (strings). Imprima o array no console.

const colors = [`Vermelho`, `Verde`, `Azul`]

console.log(colors)

// 13 -> Acesse e imprima o segundo elemento do array colors criado no exercício anterior.

console.log(colors[1])